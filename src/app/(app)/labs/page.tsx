import Link from "next/link";
import { labs } from "@/lib/labs";
import { LabProgress } from "@/components/labs/progress";
export const metadata = { title: "Interactive labs | WattWise" };
export default function LabsPage() {
  return <div className="mx-auto max-w-6xl px-4 py-10 md:px-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-[var(--blue)]">WattWise / Applied learning</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Learn by working it out.</h1><p className="mt-3 max-w-2xl text-[var(--ink-soft)]">Build a circuit, look inside the equipment, and follow the evidence. Short interactive labs bring electrical concepts into focus.</p><div className="mt-8 grid gap-4 md:grid-cols-2">{labs.map((l, i) => <Link key={l.id} href={`/labs/${l.id}`} className="group rounded-3xl bg-[#101e2e] p-7 text-white transition hover:-translate-y-1"><div className="flex items-center justify-between"><span className="text-xs tracking-widest text-white/60">LAB 0{i + 1} · {l.minutes} MIN</span><span style={{ background: l.color }} className="grid size-12 place-items-center rounded-2xl text-xl text-[#101e2e]">{["ϟ", "▥", "⌁", "⌂", "✓"][i]}</span></div><h2 className="mt-8 text-2xl font-semibold">{l.title}</h2><p className="mt-2 max-w-sm text-sm leading-6 text-white/65">{l.subtitle}</p><p className="mt-6 text-sm" style={{ color: l.color }}>Enter the lab →</p></Link>)}</div><LabProgress/><p className="text-xs text-[var(--ink-soft)]">Concept trainers with simplified models. Technical content awaits independent instructor review. Completion is study progress, not a field-work qualification.</p></div>;
}
